import { Container } from './S_Home'
import React, {memo} from 'react'

const P_Home = ({}) => {
    return (
        <Container>
            <h1>home</h1>
        </Container>
    )
}

export default memo(P_Home)